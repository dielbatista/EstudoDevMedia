/**
 * como funciona a logica o Curto circuito
 */
const exe1 = null || "exemplo" //exemplo no operador ||
console.log(exe1)

/**
 * Para entender melhor, no operador || funciona forma que se o primeiro valor da esquerda para direita atribuida ao curto circuito for false, o valor atribuido 
 * é o valor da direita a variavel, porem se o primeiro valor for true, ja é retornado para variavel o proprio valor true.
 * 
 *  
 */