/**
 * Quando é necessario substituir uma substring dentro
 * de um texto para outra substrig, podemos usar o metodo replace()
 */

let texto = "A linguagem PHP é ótima" 
let resultado = texto.replace("PHP", "Javascript")
/**
 * Acima temos o metodo replace() em ação, no caso ele substituirá 
 * a Substring "PHP", para "Javascript"
*/
console.log(resultado)//Resultado abaixo
//A linguagem Javascript é ótima